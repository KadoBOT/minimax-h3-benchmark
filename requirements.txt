# Runtime dependencies for the H3 Lab backend.
# Versions are floors, not pins: this is a single-user local tool, and pinning it to the
# day it was written only makes the next `pip install` a downgrade.

fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
httpx>=0.27
websockets>=12.0
python-multipart>=0.0.9
